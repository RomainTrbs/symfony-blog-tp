{% extends 'base.html.twig' %}

{% block title %}{{'New Page'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new Page'|trans}}</h1>

    {{ include('page/_form.html.twig') }}

    <a type="button" class="btn btn-outline-dark" href="{{ path('app_page_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
